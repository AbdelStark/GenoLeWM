# Release workflow: tag → build → PyPI / TestPyPI → GitHub release.
#
# Triggers:
#   - push of `vX.Y.Z` / `vX.Y.Z.devN` / `vX.Y.Z.rcN` tag → PyPI
#   - manual dispatch with `target=testpypi` for dry-runs
#
# Uses PyPI Trusted Publishing (OIDC); no API tokens stored in
# repository secrets. The publisher claim is bound to this workflow
# file path, so renaming this file requires regenerating the
# trusted-publisher config on PyPI.
name: Release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+.dev[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+rc[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+a[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+b[0-9]+"
  workflow_dispatch:
    inputs:
      target:
        description: "Where to publish"
        required: true
        default: "testpypi"
        type: choice
        options:
          - testpypi
          - pypi

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  build:
    name: Build distributions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # for Hatch to see the version tag context

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install build tooling
        run: python -m pip install build twine

      - name: Verify tag matches package version
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          tag="${GITHUB_REF##*/}"
          tag="${tag#v}"
          python -m pip install -e .
          pkg=$(python -c "import geno_lewm; print(geno_lewm.__version__)")
          echo "tag=$tag pkg=$pkg"
          if [ "$tag" != "$pkg" ]; then
            echo "::error::Tag $tag does not match package version $pkg"
            exit 1
          fi

      - name: Build
        run: python -m build

      - name: Twine check
        run: twine check --strict dist/*

      - name: Generate CycloneDX SBOM
        run: |
          python -m pip install cyclonedx-bom
          cyclonedx-py environment --output-format json --output-file dist/sbom.cdx.json

      - name: Compute artifact digests
        id: digests
        run: |
          cd dist
          sha256sum *.tar.gz *.whl *.json > SHA256SUMS
          echo "hashes=$(sha256sum *.tar.gz *.whl | base64 -w0)" >> "$GITHUB_OUTPUT"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 30
    outputs:
      hashes: ${{ steps.digests.outputs.hashes }}

  # SLSA provenance attestation for the built distributions.
  provenance:
    name: SLSA provenance
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    permissions:
      actions: read
      id-token: write
      contents: write
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v2.0.0
    with:
      base64-subjects: "${{ needs.build.outputs.hashes }}"
      provenance-name: geno-lewm.intoto.jsonl
      upload-assets: true

  publish-testpypi:
    name: Publish to TestPyPI
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.target == 'testpypi'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: testpypi
      url: https://test.pypi.org/p/geno-lewm
    permissions:
      id-token: write  # OIDC for trusted publishing
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          skip-existing: true

  publish-pypi:
    name: Publish to PyPI
    if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && github.event.inputs.target == 'pypi')
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/geno-lewm
    permissions:
      id-token: write  # OIDC for trusted publishing
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          skip-existing: false

  github-release:
    name: Draft GitHub release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build, publish-pypi]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - name: Extract release notes
        id: notes
        run: |
          tag="${GITHUB_REF##*/}"
          version="${tag#v}"
          {
            echo "## ${version}"
            echo ""
            awk -v v="$version" '
              /^## \[/{
                if (capture) exit
                if (index($0, "[" v "]")) { capture=1; next }
              }
              capture' CHANGELOG.md
          } > release_notes.md
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ steps.notes.outputs.tag }}" \
            --title "GenoLeWM ${{ steps.notes.outputs.tag }}" \
            --notes-file release_notes.md \
            --verify-tag \
            dist/*
